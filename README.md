# Project:
**...**
- HTML
- CSS
- GIT/GITHUB
- GITIGNORE
- README(info about your project)
---
##  :FIGMA:
| **Design** | [Figma](https://www.figma.com/design/CDiHtyHEBuwtt75K9McHnM/Landguru--?node-id=1-2) |

---
# extrainformaton 
- (377 line of code)
- project can be used on both telephones and computers
---
## Installation
Clone the repository and install dependencies:
```bash
git init
git add <filename> or git add .
git commit -m "Your commit message"
git remote add origin <repo-URL>
git push origin main
git pull origin main
...
git clone <repo-URL>
git status
git log
## BRANCH
git branch (in which branch you are)
git branch <branch-name>
git checkout <branch-name>
git branch -b <branch-name>(creat new branch and save)
git checkout -c <branch-name>(creat new branch and save)
git switch <branch-name>
